# The Living Table

Standalone Next.js design experiment. Uses the parent workspace's installed dependencies and a public-directory junction to its existing assets. No production routes or components are involved.

From this directory: `npm run dev`. Open http://localhost:3100.

All portions, ingredient amounts and save states are local illustrative interactions. External links lead to the public CookPilot website. No backend is connected.
